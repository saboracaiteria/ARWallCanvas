package com.arwallcanvas

import android.app.Application

class ARWallCanvasApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Inicialização futura: ARCore session manager, depth cache, etc.
    }
}
